<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title> <!-- Uses the title helper on application_helper.rb -->
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
  <%= render 'layouts/stylesheets' %>
</head>
<body>
    <div class="container">
      <%= render 'layouts/header' %>
      <section class="top_rounded">
        <%= yield %>
      </section>
       <%= render 'layouts/footer' %>
    </div>
  </body>
</html>
